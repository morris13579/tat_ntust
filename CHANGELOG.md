# 更新紀錄

本檔案記錄每個版本對使用者有感的變化。格式參考
[Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版號採
[語意化版本](https://semver.org/lang/zh-TW/)。

分類：**新增**（新功能）、**修正**（修好的問題）、**變更**（既有行為改變）、
**內部**（使用者看不到，但值得記一筆）。

## [2.1.0] - 未發布

### 新增

- **校內信箱。** 直接連學校的信件伺服器收發，不再開網頁版。第一次要另外設定
  信箱密碼——那是學校給信箱的一組獨立密碼，和登入 App 的那一組不是同一個。
  - **讀信。** 資料夾與未讀數、依日期分組的清單、捲到底自動往下載，搜尋可以
    只找目前這個資料夾或整個信箱。學校寄來的信大半是 Outlook 或 Word 產的，
    樣式寫在 `<style>` 區塊裡，App 會先把它展開成行內樣式才算繪，不然整封信
    等於沒有排版。
  - **遠端圖片預設不載入。** 信件是不受信任的內容，那些 `<img>` 有相當比例是
    追蹤像素——載下去等於把「這封信被讀了、什麼時候讀的、從哪個 IP」回報給
    寄件者。要看的話按一下「顯示圖片」，而且只放行這一次。
  - **寄信。** 編輯器就地嵌在寫信頁，不用跳去另一頁再跳回來；可以加附件、
    回覆、全部回覆與轉寄。收件者打逗號或分號就收成一顆籤，格式不對的那一顆
    當場變紅，不必等按了寄出才知道；打兩三個字會從以前往來過的人裡面找。
  - **寄出去之後五秒內可以收回。** 按下寄出不是立刻送，而是先排進寄件匣，
    五秒後才交給伺服器。那五秒是收回唯一能成立的時間——信一旦送出去就追不
    回來了。提示會帶著「收回」跟著你走，切到別的分頁也還在。
  - **滑動選單。** 往右滑標記已讀或未讀，往左滑是更多、封存、刪除；長按整列
    會跳出同一組動作，另外還可以移到自己的資料夾。刪除是搬到回收筒，救得
    回來，所以不再多問一次。
  - **App 開著的時候會盯有沒有新信**，來了從上緣落一條可以點的橫幅。**背景
    通知這一版還沒有**——這台伺服器不會主動推，只能自己問，放到背景一直問
    會吃電。

- **空教室。** 教室借用系統回答的是「每一間教室的一整天」，要自己在幾十列乘
  十四欄裡掃空白格。這一頁把它反過來：先回答「現在這棟哪幾間空著、可以待到
  幾點」。
  - **兩種檢視。** 清單依樓層列出空著的教室，每一列先講空到幾點，課名擺第二
    行前面加「下一堂」——課名沒有時態，寫在前面會被讀成「這間正在上課」。
    一整天保留原本一眼看完的形狀，橫捲時教室編號那一欄釘住不動。切換檢視不會
    把你選的大樓、日期與節次重設掉。
  - **挑時段。** 往後兩週內的任一天、任一節（借用系統的表也只排到那裡）。
    節次一律帶時間，因為沒人記得第七節是幾點。清單還可以只看連續兩節、三節
    或整天都空的。
  - **一次只查得到一棟，所以畫面會講出來。** 目前這棟是幾點抓的寫在最底下，
    其他棟列在下面、點了才去抓，各自標自己的抓取時間——不假裝這是一份即時的
    全校資料。
  - **三個入口**：課表上的空堂格（時間與星期都已經在畫面上，點下去直接開在
    那一節）、「更多」頁最上面、以及資訊系統的「校園資訊」分類。
  - 借用系統只排上課日，**週末與假日一列都不回**。那種情況會直接說沒有資料，
    不會畫成「全部空著」——那一天有沒有人借用，站台根本沒講。

### 變更

- **底部的提示膠囊只剩一種樣子。** 「取得課表中…」那顆進度提示和動作完成的
  提示先前是兩份各自手寫的東西，圓角、內距、離導覽列多遠、有沒有動畫全都不
  一樣，而它們會在同一個位置先後出現。現在是同一顆，而且新的提示直接蓋掉上
  一句，不會疊成一疊。
- **導覽列多了「信箱」一格**，排在課表後面。

### 修正

- **課表格子的選單，沒有教室的課會在時間前面空一塊。** 教室名稱組字時每一筆都
  補了一個尾隨空白，沒有教室時組出來的是單一個空白——判斷「有沒有教室」因此
  永遠成立，畫面上卻什麼都沒有。
- **同一列的教室與上課時間沒有對齊**（兩者字級不同，先前對齊的是行框的底而不是
  文字），以及**授課老師與課號兩列高度不一致**（課號那一列多了兩顆動作鈕）。
- **信箱設定頁的密碼欄，字沒有和上面的帳號切齊**，而且還沒填字時提示字比帳號
  小一號。

### 內部

- 信件快取改用 SQLite。`CacheStore` 底下是 SharedPreferences 的單一 JSON
  blob，每次讀寫都要把整包序列化一遍；一個資料夾放幾百封，那包 blob 每次開頁
  都要整份解一次。
- 這台伺服器的 `IDLE` 收得下指令也回 `+ idling`，但實測掛 90 秒之後從另一條
  連線塞一封信進去，這一頭一個位元組都沒收到；一送 `NOOP` 立刻回
  `* 2 EXISTS`。所以新信只能自己問，不能掛長連線等。
- `enough_convert` 的 Big5 少了 14 個碼位，其中包含「告」——公告類的主旨很容易
  踩到。補在 `MailTextDecoder` 裡。
- 信箱相關新增 45 個測試檔。
- **OIDC 交握終於有一份做得出來的實作。** `cour01` 用的是
  `response_mode=form_post`：ssoam2 不是用 302 把授權碼送回來，而是回一頁帶著
  自動送出表單的 HTML，跟著轉址跟不到終點，那張表單要自己再 POST 一次。
  選課系統卡在同一關（同一套流程、只差 `client_id`），要救它照抄
  `ClassroomConnector` 即可。
- **需要 cookie 的轉址鏈不可以交給 Dio 自動跟。** Dio 的自動轉址底下是 dart:io
  的 `HttpClient`，攔截器只在最外層那一次請求跑一遍，`CookieManager` 完全不
  參與中途的每一站：跨主機那一站不會被帶上網域 cookie，中途回的 `Set-Cookie`
  也不會被存起來。交握因此會安靜地失敗，而且長得跟「憑證過期」一模一樣。
  新增 `ConnectorParameter.followRedirects` 給這種情況用。
- 節次的時刻表與顯示名稱抽成 `lib/src/config/section_time.dart` 一份，
  `CourseTableControl` 的 `timeList` 與 `sectionStringList` 改由它推導。
- 空教室相關新增 5 個測試檔。

## [2.0.1] - 2026-09-11

### 變更

- **模擬排課的底部摘要可以展開了。** 點一下攤開目前選了哪些課，一門課跨幾節
  也只列一次，移除也在這裡。原本那個摺疊只是多顯示／少顯示一行字，藏掉的還
  正好是「加上實際課表共幾學分、幾處衝堂」。
- **登出後直接回到登入頁。** 先前是跳回課表分頁，靠「請先登入」的錯誤畫面撐著，
  等於登出後還站在一個沒有資料的殼裡。

### 新增

- **課程搜尋可以挑節次了。** 勾出你有空的格子，只留下完全落在那些節次裡的課。
  （querycourse 的節次篩選本來就不在伺服器端，官方網站也是拿回結果自己比對的。）
- **搜尋得到體育課了。** 體育不在任何學院的系所清單裡，選單永遠選不到它，
  現在收在「其他」學院底下。1151 學期有 139 門。

### 修正

- **下載過的檔案每次點都重抓一次。** 快取判斷寫了，但開檔成功之後沒有結束，
  照樣往下重新下載。現在命中就直接開；而且會先用檔名試一次，離線也開得起來。
- **課程搜尋一進去是空白的。** 現在預設列出你自己系所這學期開的課，而且
  「只看不衝堂」預設打開——加退選現場問的就是「哪些我排得進去」。關鍵字欄位
  會填上系所代碼，想看全部就把它清掉、把籤關掉。（課號前兩碼就是系所，所以
  不必多問一次伺服器；空條件不會查，而全校一學期 4282 門實測要 87 秒。）
- **課表的節次標籤整個下午少一節。** 臺科的節次是 `1…10 A…D`，中午
  12:20–13:10 就是第五節；App 先前把畫面標籤直接等同內部代號，於是 13:20
  被標成第五節、15:30 被標成第七節，一路錯到 17:30。現在 12:20 顯示第五節、
  15:30 第八節、17:30 第十節，與學校課程查詢系統一致。

### 內部

- `tool/release.sh` 打包 `all` 時，切換平台前的 `flutter clean` 會把剛產生的
  AAB 一起刪掉，最後卻照樣印出路徑，看起來像成功。改成產生後立刻複製到
  `dist/` 並帶上版本號。

## [2.0.0] - 2026-09-09

整個 App 重新設計，並補上幾件以前只能開電腦做的事。

### 新增

- **模擬排課。** 加退選前先把想選的課丟進草稿：衝堂即時標出來、學分自動
  累加，搜尋課程時可以只看不衝堂的。草稿與實際課表完全分開，不會互相影響。
- **課表分享。** 產生 QR 或代碼給同學掃，也可以掃同學的存成快照，約時間不用
  再互傳截圖。QR 只帶學號、學年期與課號，不含成績與作業。
- **多課表切換。** 我的課表、他人課表、模擬課表隨時切換，另有管理頁。
- **手機上繳交作業。** 檔案、線上文字、限時作答、沿用上次繳交、團體作業與
  補交都支援，也可以刪除已繳交的內容。
- **討論區可以發文了。** 發表新討論、回覆、加附件、編輯與刪除，改用所見即所得
  編輯，排版與內嵌圖片都留得住。
- **公告與通知頁。** TAT 公告與 Moodle 站內通知集中一處。
- **跨課程作業待辦。** 所有課程的作業集中成一條時間軸，依到期遠近分組。
- **Moodle 補完。** 各課程總分一覽、測驗資訊、資料夾子目錄、依副檔名顯示的
  檔案圖示、更換個人頭貼。
- **內建瀏覽器重做。** 網址列顯示目前站台，離開學校網域時提示，開啟校內頁面
  自動帶登入狀態。

### 變更

- 全 App 圖示改用 Lucide，深色模式完整支援，載入不再整頁蓋住畫面。
- 重寫隱私權條款，改為說明 App 實際會碰到哪些資料。
- 版本更新改用商店本身的提示，移除自家的版本檢查。
- 統一 iOS 與 Android 的啟動畫面。

### 修正

- **Android 12+ 上所有卡片都看不見。** 動態配色交出來的色表沒有 M3 的
  `surfaceContainer` 階層，底色與卡片色因而變成同一個顏色，整頁只剩一塊底色。
- **不支援 16 KB page size。** 掃描 QR 用到的兩個原生程式庫只有 4 KB 對齊，
  在 Android 15 上會跳相容性警告。
- 修正語言切換、登入錯誤攔截，以及成績與學期清單。

### 內部

- Task 層改寫為 repository + `Result`，登入收斂到 `AuthSession`，分層循環歸零。

[2.1.0]: https://github.com/SeiElikA/tat_ntust/compare/v2.0.0...HEAD
[2.0.1]: https://github.com/SeiElikA/tat_ntust/compare/v2.0.0...HEAD
[2.0.0]: https://github.com/SeiElikA/tat_ntust/releases/tag/v2.0.0
