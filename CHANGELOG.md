# 更新紀錄

本檔案記錄每個版本對使用者有感的變化。格式參考
[Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版號採
[語意化版本](https://semver.org/lang/zh-TW/)。

分類：**新增**（新功能）、**修正**（修好的問題）、**變更**（既有行為改變）、
**內部**（使用者看不到，但值得記一筆）。

## [2.0.1] - 未發布

### 變更

- **模擬排課的底部摘要可以展開了。** 點一下攤開目前選了哪些課，一門課跨幾節
  也只列一次，移除也在這裡。原本那個摺疊只是多顯示／少顯示一行字，藏掉的還
  正好是「加上實際課表共幾學分、幾處衝堂」。
- **登出後直接回到登入頁。** 先前是跳回課表分頁，靠「請先登入」的錯誤畫面撐著，
  等於登出後還站在一個沒有資料的殼裡。

### 新增

- **課程搜尋可以挑節次了。** 勾出你有空的格子，只留下完全落在那些節次裡的課。
  （querycourse 的節次篩選本來就不在伺服器端，官方網站也是拿回結果自己比對的。）
- **搜尋得到體育課了。** 體育不在任何學院的系所清單裡，選單永遠選不到它，
  現在收在「其他」學院底下。1151 學期有 139 門。

### 修正

- **下載過的檔案每次點都重抓一次。** 快取判斷寫了，但開檔成功之後沒有結束，
  照樣往下重新下載。現在命中就直接開；而且會先用檔名試一次，離線也開得起來。
- **課程搜尋一進去是空白的。** 現在預設列出你自己系所這學期開的課，而且
  「只看不衝堂」預設打開——加退選現場問的就是「哪些我排得進去」。關鍵字欄位
  會填上系所代碼，想看全部就把它清掉、把籤關掉。（課號前兩碼就是系所，所以
  不必多問一次伺服器；空條件不會查，而全校一學期 4282 門實測要 87 秒。）
- **課表的節次標籤整個下午少一節。** 臺科的節次是 `1…10 A…D`，中午
  12:20–13:10 就是第五節；App 先前把畫面標籤直接等同內部代號，於是 13:20
  被標成第五節、15:30 被標成第七節，一路錯到 17:30。現在 12:20 顯示第五節、
  15:30 第八節、17:30 第十節，與學校課程查詢系統一致。

### 內部

- `tool/release.sh` 打包 `all` 時，切換平台前的 `flutter clean` 會把剛產生的
  AAB 一起刪掉，最後卻照樣印出路徑，看起來像成功。改成產生後立刻複製到
  `dist/` 並帶上版本號。

## [2.0.0] - 2026-09-09

整個 App 重新設計，並補上幾件以前只能開電腦做的事。

### 新增

- **模擬排課。** 加退選前先把想選的課丟進草稿：衝堂即時標出來、學分自動
  累加，搜尋課程時可以只看不衝堂的。草稿與實際課表完全分開，不會互相影響。
- **課表分享。** 產生 QR 或代碼給同學掃，也可以掃同學的存成快照，約時間不用
  再互傳截圖。QR 只帶學號、學年期與課號，不含成績與作業。
- **多課表切換。** 我的課表、他人課表、模擬課表隨時切換，另有管理頁。
- **手機上繳交作業。** 檔案、線上文字、限時作答、沿用上次繳交、團體作業與
  補交都支援，也可以刪除已繳交的內容。
- **討論區可以發文了。** 發表新討論、回覆、加附件、編輯與刪除，改用所見即所得
  編輯，排版與內嵌圖片都留得住。
- **公告與通知頁。** TAT 公告與 Moodle 站內通知集中一處。
- **跨課程作業待辦。** 所有課程的作業集中成一條時間軸，依到期遠近分組。
- **Moodle 補完。** 各課程總分一覽、測驗資訊、資料夾子目錄、依副檔名顯示的
  檔案圖示、更換個人頭貼。
- **內建瀏覽器重做。** 網址列顯示目前站台，離開學校網域時提示，開啟校內頁面
  自動帶登入狀態。

### 變更

- 全 App 圖示改用 Lucide，深色模式完整支援，載入不再整頁蓋住畫面。
- 重寫隱私權條款，改為說明 App 實際會碰到哪些資料。
- 版本更新改用商店本身的提示，移除自家的版本檢查。
- 統一 iOS 與 Android 的啟動畫面。

### 修正

- **Android 12+ 上所有卡片都看不見。** 動態配色交出來的色表沒有 M3 的
  `surfaceContainer` 階層，底色與卡片色因而變成同一個顏色，整頁只剩一塊底色。
- **不支援 16 KB page size。** 掃描 QR 用到的兩個原生程式庫只有 4 KB 對齊，
  在 Android 15 上會跳相容性警告。
- 修正語言切換、登入錯誤攔截，以及成績與學期清單。

### 內部

- Task 層改寫為 repository + `Result`，登入收斂到 `AuthSession`，分層循環歸零。

[2.0.1]: https://github.com/SeiElikA/tat_ntust/compare/v2.0.0...HEAD
[2.0.0]: https://github.com/SeiElikA/tat_ntust/releases/tag/v2.0.0
